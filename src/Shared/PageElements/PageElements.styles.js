const styles = theme => ({
  header: {
    marginTop: 20,
    marginBottom: 40,
    color: theme.palette.colors.darkGrey,
  },
  subHeader: {
    marginTop: 20,
    marginBottom: 20,
    color: theme.palette.colors.darkGrey,
  },
  headerDivider: {
    marginBottom: 30,
  },
  text: {
    color: theme.palette.colors.darkGrey,
  },
});

export default styles;
