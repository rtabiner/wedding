const styles = theme => ({
  header: {
    marginTop: 20,
    marginBottom: 40,
    color: theme.palette.colors.darkGrey,
  },
  text: {
    color: theme.palette.colors.darkGrey,
  },
});

export default styles;
