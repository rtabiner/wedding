const styles = theme => ({
  intro: {
    marginBottom: 30,
  },  
  schedule: {
    paddingTop: 30,
    marginBottom: 50,
  },
  scheduleDivider: {
    marginBottom: 50,
  },
});

export default styles;
